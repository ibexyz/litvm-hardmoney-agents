[profile.default]
src = "contracts/src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "shanghai"
fs_permissions = [{ access = "read-write", path = "./"}]

[rpc_endpoints]
liteforge = "${RPC_URL}"

[etherscan]
# Add verification key later if needed
